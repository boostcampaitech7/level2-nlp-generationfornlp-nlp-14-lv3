from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent
SRC_PATH = Path(ROOT, "src")
DATA_PATH = Path(ROOT, "data")
EXP_PATH = Path(ROOT, "experiments")
SCRIPT_PATH = Path(ROOT, "scripts")
WIKI_PATH = Path(ROOT, "data")
